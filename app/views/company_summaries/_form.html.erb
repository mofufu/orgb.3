<%= form_for @company_summary, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <%= f.label :ownership, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :ownership, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :start_plan, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_area :start_plan, :class => 'text-field form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :locations, :class => 'control-label col-md-2' %>
    <div class="col-md-10">
      <%= f.text_field :locations, :class => 'text-field form-control' %>
    </div>
  </div>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  company_summaries_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
